import React from 'react';

function Category(props) {
	return (
		<div>Category View - identical to the default view, but 
		filtered to only include posts with the selected category</div>
	);
}

export default Category;
